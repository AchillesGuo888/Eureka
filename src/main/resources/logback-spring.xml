<?xml version="1.0" encoding="UTF-8"?><configuration scan="false" scanPeriod="60 seconds" debug="false">    <!-- 开发环境 -->    <springProfile name="local">        <property name = "LOG_PATH" value = "../data/logs/api"/>    </springProfile>    <!-- 生产环境 -->    <springProfile name="dev,test,prod">        <property name="LOG_PATH" value="/data/logs/eureka"/>    </springProfile>    <appender name="consoleAppender" class="ch.qos.logback.core.ConsoleAppender">        <encoder>            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %green([%-5level]) %highlight([%thread]) %boldMagenta(%logger{50}) : %cyan(%msg%n)</pattern>            <charset>UTF-8</charset>        </encoder>    </appender>    <appender name="runtimeAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">        <file>${LOG_PATH}/eureka-info.log</file>        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">            <maxFileSize>100MB</maxFileSize>            <!-- daily rollover -->            <fileNamePattern>${LOG_PATH}/days/%d{yyyy-MM-dd}/eureka-info.%i.log</fileNamePattern>            <!-- 保留30天的日志 -->            <maxHistory>30</maxHistory>        </rollingPolicy>        <encoder>            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %green([%-5level]) %highlight([%thread]) %boldMagenta(%logger{50}) : %cyan(%msg%n)</pattern>            <charset>UTF-8</charset>        </encoder>    </appender>    <root level="INFO">        <appender-ref ref="consoleAppender" />        <appender-ref ref="runtimeAppender" />    </root></configuration>